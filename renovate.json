{
  "$schema": "https://docs.renovatebot.com/renovate-schema.json",
  "extends": ["config:recommended"],
  "description": "pnpm workspace: Renovate scans every package.json and keeps pnpm-lock.yaml in sync. Use the same file with the hosted Renovate app on GitHub, GitLab, Bitbucket, Gitea, Azure DevOps, or with a self-hosted bot.",
  "dependencyDashboard": true,
  "dependencyDashboardTitle": "Dependency dashboard",
  "labels": ["dependencies"],
  "prConcurrentLimit": 5,
  "prHourlyLimit": 2,
  "rangeStrategy": "auto",
  "lockFileMaintenance": {
    "enabled": true,
    "schedule": ["before 6am on the first day of the month"]
  },
  "packageRules": [
    {
      "description": "Group Vitest packages into one PR",
      "matchPackageNames": ["vitest", "@vitest/coverage-v8"],
      "groupName": "vitest"
    },
    {
      "description": "Group @types/* to reduce PR noise",
      "matchPackageNames": ["/^@types\\//"],
      "groupName": "types"
    }
  ]
}

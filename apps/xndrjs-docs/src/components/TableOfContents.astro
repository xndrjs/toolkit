---
interface Props {
  headings: Array<{ depth: number; slug: string; text: string }>;
}

const { headings } = Astro.props;
---

{headings.length > 0 && (
  <nav class="table-of-contents" aria-label="Table of contents">
    <ul class="table-of-contents__list">
      {headings.map((heading) => (
        <li class={`table-of-contents__item table-of-contents__item--depth-${heading.depth}`}>
          <a href={`#${heading.slug}`} class="table-of-contents__link">
            {heading.text}
          </a>
        </li>
      ))}
    </ul>
  </nav>
)}

<style>
  .table-of-contents {
    font-size: 0.86rem;
    border-left: 1px solid var(--xndr-border-subtle);
    padding-left: 1rem;
    margin-left: 1rem;
    position: sticky;
    top: 80px;
    max-height: calc(100vh - 100px);
    overflow-y: auto;
  }

  .table-of-contents__list {
    list-style: none;
    padding: 0;
    margin: 0;
  }

  .table-of-contents__item {
    margin: 0.35rem 0;
  }

  .table-of-contents__item--depth-3 {
    padding-left: 1rem;
  }

  .table-of-contents__item--depth-4 {
    padding-left: 2rem;
  }

  .table-of-contents__link {
    font-size: 0.86rem;
    line-height: 1.45;
    transition: color 0.2s ease;
    color: var(--xndr-text-muted);
    text-decoration: none;
    display: block;
  }

  .table-of-contents__link:hover {
    color: var(--xndr-text-primary);
    text-decoration: none;
  }

  .table-of-contents__link--active {
    font-size: 0.86rem;
    font-weight: 600;
    color: var(--xndr-cyan);
    border-left-color: var(--xndr-border-subtle);
  }

  @media (max-width: 996px) {
    .table-of-contents {
      display: none;
    }
  }
</style>

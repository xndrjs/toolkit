---
"@xndrjs/resource-graph-resolver": minor
---

Rename `ResolutionStrategy` to `SchedulingMode` and rename the resolver config field from `strategy` to `schedulingMode`. Update `ResolutionStartEvent` accordingly.

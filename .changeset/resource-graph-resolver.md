---
"@xndrjs/resource-graph-resolver": minor
---

Initial release: typed content-graph resolution with islands, pull-based data ports (`process` / `take`), payload-scoped expansion policies, correlated `{ resource, payload }` load results, optional abort/limit budgets, and island serialization.

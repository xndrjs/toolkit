---
"@xndrjs/resource-graph-resolver": minor
---

Separate island boundaries from expansion: add `IslandPort`, `defineIslandPolicy`, and `createIslandPolicyChain` (OR semantics). `createResourceGraphResolver` now requires an `islands` port. Remove `isIsland` from `ExpansionResult`.
